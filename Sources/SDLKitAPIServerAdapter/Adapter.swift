#if canImport(OpenAPIRuntime) && canImport(SDLKitAPI) && canImport(SDLKit)
import OpenAPIRuntime
import SDLKitAPI
import SDLKit

/// A placeholder for a future generated-server adapter.
/// This type will conform to the generated server interfaces and delegate to SDLKitJSONAgent
/// once the server runtime is finalized in this repository. For now it compiles without being used.
public struct SDLKitAPIServerAdapter {
    public init() {}
}
#endif

